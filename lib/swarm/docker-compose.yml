version: '3.7'

networks:
  bridge:
    external: true
  bridge-net:
    external: true
  ingress:
    external: true
  traefik-net:
    external: true
  agent-net:
    external: true

volumes:
  datasets-data:
    external: true
  minio-data:
    external: true
  minio-config:
    external: true
  mc-config:
    external: true
  toil-jobstore:
    external: true
  portainer-data:
    external: true
  prometheus-data:
    external: true
  consul-data:
    external: true
  grafana-data:
    external: true

configs:
  chord-metadata-settings:
    external: true
  wes-dependency-resolver:
    external: true

secrets:
  aws-credentials:
    external: true
  metadata-app-secret:
    external: true
  metadata-db-user:
    external: true
  metadata-db-secret:
    external: true
  minio-access-key:
    external: true
  minio-secret-key:
    external: true
  portainer-user:
    external: true
  portainer-secret:
    external: true
  traefik-ssl-key:
    external: true
  traefik-ssl-crt:
    external: true

