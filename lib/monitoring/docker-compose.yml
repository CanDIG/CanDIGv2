version: '3'

services:
  prometheus:
    image: prom/prometheus:latest
    privileged: true
    volumes:
      - /Users/sonchau/Documents/GitHub/CANDIG/CanDIGv2/lib/monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.console.libraries=/usr/share/prometheus/console_libraries
      - --web.console.templates=/usr/share/prometheus/consoles
    ports:
      - '9090:9090'
    extra_hosts:
      - "${CANDIG_DOMAIN}:${LOCAL_IP_ADDR}"
 
  # blackbox:
  #   image: prom/blackbox-exporter:latest
  #   privileged: true
  #   volumes:
  #     - /Users/sonchau/Documents/GitHub/CANDIG/CanDIGv2/lib/monitoring/blackbox/blackbox.yml:/etc/blackbox_exporter/config.yml
  #   ports:
  #     - '9115:9115'
  #   extra_hosts:
  #     - "${CANDIG_DOMAIN}:${LOCAL_IP_ADDR}"
 
  # node-exporter:
  #   image: prom/node-exporter:latest
  #   privileged: true
  #   ports:
  #     - '9100:9100'
 
  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=pass
    depends_on:
      - prometheus
    ports:
      - "3000:3000"
    extra_hosts:
      - "${CANDIG_DOMAIN}:${LOCAL_IP_ADDR}"