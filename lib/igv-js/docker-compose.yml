# tested on docker-engine 18.06.0
version: '3.7'

services:
 igv-js:
    build:
      context: $PWD/lib/igv-js
    image: igv-js:${IGVJS_VERSION:-latest}
    networks:
      - ${DOCKER_NET:-bridge-net}
    ports:
      - ${IGVJS_PORT:-80}:80

